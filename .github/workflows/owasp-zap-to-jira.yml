name: OWASP ZAP to Jira - Manual workflow

on:
  workflow_dispatch:

jobs:
  workflow:
    name: example workflow - owasp zap
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Pull owasp ZAP 
      run: docker pull ghcr.io/zaproxy/zaproxy:stable
        
    - name: Install python
      uses: actions/setup-python@v5

# following parts of the workflow are just for example purpose. You can scan a url api definition that is published on the internet.
    - name: Pull image to be scanned
      run: docker pull alpine
    
    - name: Run docker image
      run: to be done

    - name: Scan the image
      run: to be done

    - name: Publish result to JIRA
      run: python ${{ github.workspace }}/scripts/owasp-ticketizer.py testresult.json ${{ secrets.JIRA }} alpine # alpine can be exchanged for repository name variable - ${{ github.event.repository.name }}
