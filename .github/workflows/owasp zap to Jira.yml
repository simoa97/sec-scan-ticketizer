name: OWASP ZAP to Jira - Manual workflow

on:
  workflow_dispatch:

jobs:
  workflow:
    name: example workflow - owasp zap
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install trivy
      run: curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /usr/local/bin v0.49.1
        
    - name: Install python
      uses: actions/setup-python@v5

    - name: Pull image to be scanned
      run: docker pull alpine

    - name: Scan the image
      run: trivy image -f json -o testresult alpine:latest

    - name: Publish result to JIRA
      run: python ${{ github.workspace }}/scripts/trivy-ticketizer.py testresult.json ${{ secrets.JIRA }} alpine # alpine can be exchanged for repository name variable - ${{ github.event.repository.name }}
